awk '
FILENAME != prevfile {
	NR=1
	prevfile=FILENAME
}
NF>0{
	if($i==lastword)
		printf "double %s, file %s, line %d\n",$1,FILENAME,NR
	for(i=2;i<=NF;i++)
		if($i==$(i-1))
			printf "double %s, file %s, line %d\n",$1,FILENAME,NR
	if(NF>0)
		lastword=$NF
}' $*
